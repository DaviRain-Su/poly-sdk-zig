# 安全政策

## 报告漏洞

如果你发现安全漏洞，请**不要**公开提交 Issue。

请发送邮件至项目维护者，包含以下信息：

1. **问题描述**和潜在影响
2. **复现步骤**或最小化的概念验证
3. **相关日志**或环境详情

我们会确认收到、调查问题，并提供后续指导。

## 安全最佳实践

使用本 SDK 时，请注意：

### 私钥保护

```zig
// ❌ 错误：硬编码私钥
const private_key = "0x1234...";

// ✅ 正确：从环境变量读取
const private_key = std.posix.getenv("POLYGON_WALLET_PRIVATE_KEY");
```

### 使用 Secret 类型

```zig
// ❌ 错误：原始字符串存储敏感数据
const api_key: []const u8 = "sk_live_xxx";

// ✅ 正确：使用 Secret 包装防止意外日志泄露
const api_key = Secret([]const u8).init("sk_live_xxx");
```

### 环境变量

- 永远不要提交 `.env` 文件
- 使用 `.env.example` 作为模板
- 生产环境使用安全的密钥管理服务

## 依赖安全

本项目目标是最小化外部依赖，降低供应链攻击风险。

## 免责声明

本软件按"原样"提供，不提供任何明示或暗示的保证。使用本软件进行交易的风险由用户自行承担。

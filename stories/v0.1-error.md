# Story: v0.1-error

> **状态**: ✅ 已完成
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.1
> **Session**: 2024-12-31-005
> **依赖**: 无

## 目标

定义 SDK 的错误类型。

## 需求

### 错误分类

```zig
pub const Error = error{
    // 网络
    ConnectionFailed,
    Timeout,
    
    // HTTP
    BadRequest,
    Unauthorized,
    Forbidden,
    NotFound,
    RateLimited,
    ServerError,
    
    // 解析
    InvalidJson,
    InvalidDecimal,
    InvalidAddress,
    
    // 验证
    InvalidPrice,
    InvalidSize,
    InvalidTokenId,
    
    // 认证
    MissingCredentials,
    InvalidSignature,
    ExpiredCredentials,
};
```

### 错误上下文

```zig
pub const ErrorContext = struct {
    code: Error,
    message: []const u8,
    http_status: ?u16 = null,
    retry_after: ?u64 = null,  // 用于速率限制
};
```

## 任务

- [x] 在 `src/error.zig` 中定义错误枚举
- [x] 添加 HTTP 状态码映射 (`fromHttpStatus`)
- [x] 添加错误上下文结构 (`ErrorContext`)
- [x] 添加错误描述 (`describe`)
- [x] 添加重试判断 (`isRetryable`)
- [x] 添加 `ResultWithContext` 类型
- [x] 测试 (14 tests)

## 文件

```
src/
└── error.zig    # ✅ 已实现
```

## 验收标准

- [x] 所有错误类型已定义（6 类 40+ 错误）
- [x] HTTP 状态正确映射到错误
- [x] 错误消息有帮助
- [x] 所有测试通过

## 错误分类

| 类别 | 数量 | 示例 |
|------|------|------|
| 网络错误 | 6 | ConnectionFailed, Timeout |
| HTTP 4xx | 8 | BadRequest, Unauthorized, RateLimited |
| HTTP 5xx | 5 | InternalServerError, ServiceUnavailable |
| 解析错误 | 7 | InvalidJson, InvalidDecimal |
| 验证错误 | 9 | InvalidPrice, InvalidSize |
| 认证错误 | 7 | MissingCredentials, InvalidSignature |
| 订单错误 | 5 | OrderNotFound, MarketClosed |
| 系统错误 | 3 | OutOfMemory, Cancelled |

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| 2024-12-31-005 | 2024-12-31 | 完整实现，14 个测试通过 |

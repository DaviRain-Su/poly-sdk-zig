# Story: v0.1-error

> **Status**: ⏳ Pending
> **Parent**: [ROADMAP.md](../ROADMAP.md) → v0.1
> **Depends on**: None

## Goal

Define error types for the SDK.

## Requirements

### Error Categories

```zig
pub const Error = error{
    // Network
    ConnectionFailed,
    Timeout,
    
    // HTTP
    BadRequest,
    Unauthorized,
    Forbidden,
    NotFound,
    RateLimited,
    ServerError,
    
    // Parsing
    InvalidJson,
    InvalidDecimal,
    InvalidAddress,
    
    // Validation
    InvalidPrice,
    InvalidSize,
    InvalidTokenId,
    
    // Auth
    MissingCredentials,
    InvalidSignature,
    ExpiredCredentials,
};
```

### Error Context

```zig
pub const ErrorContext = struct {
    code: Error,
    message: []const u8,
    http_status: ?u16 = null,
    retry_after: ?u64 = null,  // For rate limiting
};
```

## Tasks

- [ ] Define error enum in `src/error.zig`
- [ ] Add HTTP status code mapping
- [ ] Add error context struct
- [ ] Tests

## Files

```
src/
└── error.zig    # ⏳ To be created
```

## Acceptance Criteria

- [ ] All error types defined
- [ ] HTTP status maps to correct error
- [ ] Error messages are helpful

# Story: v0.2-orders

> **状态**: ⏳ 待开始
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.2
> **Session**: -
> **依赖**: v0.2-order-builder, v0.2-l2-auth

## 目标

实现订单管理 API，支持发布、查询、取消订单，以及交易历史、余额查询等功能。

## 需求

完整的订单管理功能：
1. 发布订单（单个/批量）
2. 查询订单
3. 取消订单
4. 交易历史
5. 账户余额
6. 通知管理

```zig
const client = @import("clob").Client;

// 初始化带认证的客户端
var clob = try client.init(allocator, .{
    .host = "https://clob.polymarket.com",
    .wallet = wallet,
    .api_creds = creds,
});
defer clob.deinit();

// 创建并发布订单
const signed_order = try clob.createOrder(.{
    .token_id = "123456789",
    .price = try Decimal.fromString("0.65"),
    .size = try Decimal.fromString("100"),
    .side = .BUY,
}, .{ .tick_size = .@"0.01" });

const result = try clob.postOrder(signed_order, .GTC);

// 查询订单
const orders = try clob.getOpenOrders(.{});
const order = try clob.getOrder(order_id);

// 取消订单
try clob.cancelOrder(order_id);
try clob.cancelAll();

// 交易历史
const trades = try clob.getTrades(.{});

// 余额查询
const balance = try clob.getBalanceAllowance(.{
    .asset_type = .COLLATERAL,
});
```

## 任务

### 订单发布
- [ ] 实现 `postOrder(signed_order, order_type)` - POST /order
- [ ] 实现 `postOrders(signed_orders, order_type)` - POST /orders
- [ ] 请求体 JSON 序列化
- [ ] 响应解析

### 便捷方法
- [ ] 实现 `createAndPostOrder(args, options)` - 创建 + 发布
- [ ] 实现 `createAndPostMarketOrder(args, options)` - 市价单
- [ ] 组合 OrderBuilder + postOrder

### 订单查询
- [ ] 实现 `getOpenOrders(params)` - GET /data/orders
- [ ] 实现 `getOrder(order_id)` - GET /data/order/{id}
- [ ] 分页支持
- [ ] 过滤参数

### 订单取消
- [ ] 实现 `cancelOrder(order_id)` - DELETE /order
- [ ] 实现 `cancelOrders(order_ids)` - DELETE /orders
- [ ] 实现 `cancelAll()` - DELETE /cancel-all
- [ ] 实现 `cancelMarketOrders(market)` - DELETE /cancel-market-orders

### 交易历史
- [ ] 实现 `getTrades(params)` - GET /data/trades
- [ ] 实现 `getTradesPaginated(params)` - 分页获取
- [ ] 过滤参数支持

### 账户操作
- [ ] 实现 `getBalanceAllowance(params)` - GET /balance-allowance
- [ ] 实现 `updateBalanceAllowance()` - GET /balance-allowance/update
- [ ] COLLATERAL 和 CONDITIONAL 资产类型

### API Key 管理
- [ ] 实现 `getApiKeys()` - GET /auth/api-keys
- [ ] 实现 `deleteApiKey()` - DELETE /auth/api-key
- [ ] 实现 `getClosedOnlyMode()` - GET /auth/ban-status/closed-only

### 通知
- [ ] 实现 `getNotifications()` - GET /notifications
- [ ] 实现 `dropNotifications(ids)` - DELETE /notifications

### 响应类型
- [ ] 实现 `PostOrderResponse` 类型
- [ ] 实现 `OpenOrder` 类型
- [ ] 实现 `Trade` 类型
- [ ] 实现 `BalanceAllowanceResponse` 类型
- [ ] 实现 `Notification` 类型

### 测试
- [ ] 订单发布测试（模拟 API）
- [ ] 订单查询测试
- [ ] 取消订单测试
- [ ] 交易历史测试
- [ ] 余额查询测试
- [ ] 集成测试（需要 testnet）

### 文档
- [ ] 创建 `docs/order/api.md`
- [ ] 端点使用说明
- [ ] 错误处理指南

## 文件

```
src/
└── clob/
    ├── client.zig        # 更新，添加认证端点
    ├── orders.zig        # ⏳ 订单管理方法
    ├── trades.zig        # ⏳ 交易相关方法
    ├── account.zig       # ⏳ 账户相关方法
    └── types/
        ├── order.zig     # ⏳ 订单响应类型
        ├── trade.zig     # ⏳ 交易响应类型
        └── account.zig   # ⏳ 账户响应类型
```

## 技术细节

### 订单发布请求

```json
POST /order
{
    "order": {
        "salt": "123456789",
        "maker": "0x...",
        "signer": "0x...",
        "taker": "0x0000000000000000000000000000000000000000",
        "tokenId": "123456789",
        "makerAmount": "65000000",
        "takerAmount": "100000000",
        "expiration": "0",
        "nonce": "0",
        "feeRateBps": "0",
        "side": "BUY",
        "signatureType": 0,
        "signature": "0x..."
    },
    "orderType": "GTC"
}
```

### 订单响应

```json
{
    "success": true,
    "errorMsg": "",
    "orderID": "0x...",
    "transactionsHashes": ["0x..."]
}
```

### 查询参数

```zig
pub const OpenOrderParams = struct {
    id: ?[]const u8 = null,
    market: ?[]const u8 = null,
    asset_id: ?[]const u8 = null,
};

pub const TradeParams = struct {
    id: ?[]const u8 = null,
    maker_address: ?[]const u8 = null,
    market: ?[]const u8 = null,
    asset_id: ?[]const u8 = null,
    before: ?[]const u8 = null,
    after: ?[]const u8 = null,
};
```

### 分页

```zig
pub const PaginationPayload = struct {
    limit: u32,
    count: u32,
    next_cursor: []const u8,
    data: []OpenOrder,
};

// 初始游标
const INITIAL_CURSOR = "MA==";
// 结束游标
const END_CURSOR = "LTE=";
```

### 错误处理

| HTTP 状态 | 错误 | 处理 |
|-----------|------|------|
| 400 | 无效订单 | 检查订单参数 |
| 401 | 认证失败 | 检查 API Key |
| 403 | 无权限 | 检查账户状态 |
| 404 | 订单不存在 | 确认订单 ID |
| 429 | 请求过多 | 添加重试逻辑 |

## 验收标准

- [ ] 能成功发布限价单
- [ ] 能成功发布市价单
- [ ] 能查询开放订单
- [ ] 能取消单个订单
- [ ] 能取消所有订单
- [ ] 能获取交易历史
- [ ] 能查询账户余额
- [ ] 分页功能正常
- [ ] 错误处理完善
- [ ] 所有测试通过：`zig build test`

## 参考资料

- [py-clob-client/client.py](https://github.com/Polymarket/py-clob-client/blob/main/py_clob_client/client.py)
- [clob-client/src/client.ts](https://github.com/Polymarket/clob-client/blob/main/src/client.ts)
- [Polymarket API 文档](https://docs.polymarket.com/)

## 备注

- 所有写操作需要 L2 认证
- 订单发布后需要等待确认
- 取消订单可能需要链上交易
- 市价单需要先获取订单簿
- 考虑添加订单状态轮询功能
- 批量操作有数量限制（通常 100）

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| - | - | 待开始 |

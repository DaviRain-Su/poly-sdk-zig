# Story: v1.0-release

> **状态**: 🔨 进行中
> **父级**: [ROADMAP.md](../ROADMAP.md) → v1.0
> **Session**: 2026-01-01-001
> **依赖**: v0.1 ~ v0.5 全部完成

## 目标

准备 v1.0 稳定版发布，确保 API 100% 覆盖、文档完整、示例丰富、CI/CD 配置完成。

## 需求

v1.0 是第一个生产就绪版本，需要：

1. **API 覆盖**: 所有 Polymarket CLOB API 端点都已实现
2. **文档完整**: 所有模块都有对应的文档
3. **示例丰富**: 提供实用的示例代码
4. **CI/CD**: GitHub Actions 自动测试
5. **发布准备**: 可以发布到 Zig 包管理器

```zig
// v1.0 使用示例
const poly = @import("poly-sdk-zig");

pub fn main() !void {
    var gpa = std.heap.GeneralPurposeAllocator(.{}){};
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    // 1. 创建客户端
    var client = poly.ClobClient.init(allocator, .{});
    defer client.deinit();

    // 2. 公共 API (L0)
    const markets = try client.getMarkets(.{});
    defer markets.deinit();

    // 3. L1 认证 - 获取 API 凭证
    const wallet = try poly.Wallet.fromPrivateKeyHex("0x...");
    client.setWallet(&wallet);
    var creds = try client.createOrDeriveApiKey();
    defer creds.deinit();
    client.setApiCreds(&creds);

    // 4. L2 认证 - 创建订单
    const order = try client.createOrder(.{
        .token_id = markets.value[0].tokens[0].token_id,
        .price = try poly.Decimal.fromString("0.65"),
        .size = try poly.Decimal.fromString("100"),
        .side = .buy,
    });
    _ = try client.postOrder(&order, .GTC);
}
```

## 任务

### 高优先级
- [x] L1 认证端点实现 (createApiKey, deriveApiKey, createOrDeriveApiKey)
- [x] Readonly API Key 端点实现 (4个)
- [x] 采样市场端点实现 (getSamplingMarkets, getSamplingSimplifiedMarkets)
- [x] ROADMAP.md 状态同步
- [ ] 创建 v1.0 Story 文件 (本文件)
- [ ] 配置 GitHub Actions CI/CD

### 中优先级
- [ ] 完善 examples/ 目录
  - [ ] 公共 API 示例 (markets, orderbook, prices)
  - [ ] 认证示例 (L1/L2)
  - [ ] 订单管理示例
  - [ ] WebSocket 示例
- [ ] 更新 README.md

### 低优先级
- [ ] ContractConfig 类型实现
- [ ] POLY_PROXY 签名类型支持
- [ ] POLY_GNOSIS_SAFE 签名类型支持

## 文件

```
.github/
└── workflows/
    └── ci.yml              # ⏳ 待创建

examples/
├── basic_types.zig         # ✅ 已存在
├── public_api.zig          # ⏳ 待创建
├── authentication.zig      # ⏳ 待创建
├── order_management.zig    # ⏳ 待创建
└── websocket.zig           # ⏳ 待创建

src/types/
└── contracts.zig           # ⏳ 待创建 (ContractConfig)
```

## 验收标准

- [x] 所有 356+ 测试通过
- [ ] GitHub Actions CI 配置完成
- [ ] examples/ 至少有 4 个示例文件
- [ ] README.md 包含快速开始指南
- [ ] 所有公共 API 有文档注释

## API 覆盖统计

| 类别 | 端点数 | 已实现 | 完成率 |
|------|--------|--------|--------|
| 公共 API (L0) | ~20 | ~20 | 100% |
| L1 认证 | 3 | 3 | 100% |
| L2 认证 | ~25 | ~25 | 100% |
| Builder | 4 | 4 | 100% |
| RFQ | 10 | 10 | 100% |
| WebSocket | 4 | 4 | 100% |
| 奖励/分析 | ~15 | ~15 | 100% |
| **总计** | **~81** | **~81** | **100%** |

## 备注

- Zig 版本要求: 0.15.2+
- 当前测试数: 356
- 所有核心 API 已实现，仅剩低优先级的签名类型扩展

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| 2026-01-01-001 | 2026-01-01 | L1端点、Readonly API Key、采样市场端点完成 (356 tests) |

# Story: v0.2-crypto

> **状态**: ✅ 已完成
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.2
> **Session**: 2024-12-31-007
> **依赖**: v0.1-types (Address, Secret)

## 目标

实现 Polymarket 认证所需的加密原语：Keccak256 哈希、secp256k1 椭圆曲线签名、HMAC-SHA256。

## 需求

Polymarket API 认证需要以下加密操作：

1. **Keccak256** - 用于 EIP-712 消息哈希、地址计算
2. **secp256k1** - 用于签名生成和验证（ECDSA）
3. **HMAC-SHA256** - 用于 L2 API 请求签名

```zig
const crypto = @import("crypto");

// Keccak256 哈希
const hash = crypto.keccak256("hello");
// => 0x1c8aff950685c2ed4bc3174f3472287b56d9517b9c948127319a09a7a36deac8

// 从私钥派生公钥
const private_key = try crypto.PrivateKey.fromHex("0x...");
const public_key = private_key.publicKey();

// 签名消息
const signature = try private_key.sign(message_hash);

// 验证签名
try public_key.verify(message_hash, &signature);

// HMAC-SHA256
const hmac = crypto.hmacSha256(secret, message);
```

## 任务

### Keccak256
- [x] 封装 `std.crypto.hash.sha3.Keccak256`
- [x] 提供便捷的 `keccak256(data)` 函数
- [x] 支持流式哈希（`Hasher` 类型）
- [x] 测试向量验证（11 tests）

### secp256k1
- [x] 使用 Zig std 的 `std.crypto.ecc.Secp256k1` + `std.crypto.sign.ecdsa`
- [x] 实现 `PrivateKey` 类型
  - `fromHex()` - 从十六进制字符串创建
  - `fromBytes()` - 从字节数组创建
  - `publicKey()` - 派生公钥
  - `sign()` - ECDSA 签名
  - `generate()` - 生成随机私钥
- [x] 实现 `PublicKey` 类型
  - `toAddress()` - 计算以太坊地址
  - `verify()` - 验证签名
  - `toUncompressedBytes()` / `toCompressedBytes()`
- [x] 实现 `Signature` 类型
  - `r`, `s`, `v` 分量
  - `toHex()` / `fromHex()` - 十六进制序列化
  - `toBytes()` / `fromBytes()` - 字节序列化
  - `getEthereumV()` - 以太坊格式 v 值

### HMAC-SHA256
- [x] 封装 `std.crypto.auth.hmac.sha2.HmacSha256`
- [x] 提供便捷的 `hmacSha256(key, message)` 函数
- [x] 支持流式计算（`Hmac` 类型）
- [x] Base64 编码支持（`toBase64`, `toBase64Alloc`）
- [x] 常量时间验证（`verify`）

### 测试
- [x] Keccak256 测试向量（11 tests）
- [x] secp256k1 签名/验证测试（13 tests）
- [x] HMAC-SHA256 测试向量（10 tests）
- [x] 已知地址测试（Vitalik's address）
- [x] 端到端签名流程测试

### 文档
- [x] 模块级文档注释
- [x] 函数级文档注释
- [ ] 创建 `docs/crypto/README.md`（可选）

## 文件

```
src/
└── crypto/
    ├── mod.zig           # ✅ 模块导出（3 tests）
    ├── keccak.zig        # ✅ Keccak256 封装（11 tests）
    ├── ecdsa.zig         # ✅ secp256k1 ECDSA（13 tests）
    └── hmac.zig          # ✅ HMAC-SHA256 封装（10 tests）
```

## 技术决策

### 使用 Zig std.crypto

经过调研，Zig 0.15 的 `std.crypto` 完全支持我们需要的功能：

```zig
// Keccak256 - 可用
const Keccak256 = std.crypto.hash.sha3.Keccak256;

// HMAC-SHA256 - 可用
const HmacSha256 = std.crypto.auth.hmac.sha2.HmacSha256;

// secp256k1 ECDSA - 可用，支持自定义哈希函数
const EcdsaSecp256k1Keccak256 = std.crypto.sign.ecdsa.Ecdsa(
    std.crypto.ecc.Secp256k1,
    std.crypto.hash.sha3.Keccak256,
);
```

**优点**：
- 无外部依赖
- 纯 Zig 实现
- 与项目其他部分一致

### 以太坊地址计算

```
address = keccak256(public_key[1:65])[12:32]
```

公钥是 65 字节（未压缩格式，包含 0x04 前缀），去掉前缀后取 Keccak256 哈希的后 20 字节。

## 验收标准

- [x] `keccak256("hello")` 返回 `0x1c8aff950685c2ed4bc3174f3472287b56d9517b9c948127319a09a7a36deac8`
- [x] 能从私钥派生出正确的以太坊地址（已验证 Vitalik's address）
- [x] 签名可以被验证
- [x] HMAC-SHA256 输出与 Python 一致
- [x] 所有测试通过：`zig build test` (139 tests)
- [x] 无内存泄漏

## 测试统计

| 模块 | 测试数 |
|------|--------|
| keccak.zig | 11 |
| hmac.zig | 10 |
| ecdsa.zig | 13 |
| mod.zig | 3 |
| **总计** | **37** |

## 参考资料

- [EIP-712: Typed structured data hashing and signing](https://eips.ethereum.org/EIPS/eip-712)
- [Ethereum Yellow Paper - Appendix F](https://ethereum.github.io/yellowpaper/paper.pdf)
- [py-clob-client/signing.py](https://github.com/Polymarket/py-clob-client/blob/main/py_clob_client/signing.py)
- [clob-client/src/signing/](https://github.com/Polymarket/clob-client/tree/main/src/signing)

## 备注

- 使用 Zig std.crypto，无需外部依赖
- 私钥可以使用 `Secret` 类型包装（在 v0.2-signer 中实现）
- 恢复 ID (v) 计算使用穷举法（尝试 0 和 1）

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| 2024-12-31-007 | 2024-12-31 | ✅ 完成所有任务，37 tests 通过 |

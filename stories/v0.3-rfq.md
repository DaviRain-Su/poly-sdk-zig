# Story: v0.3-rfq

> **状态**: ⏳ 待开始
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.3
> **Session**: -
> **依赖**: v0.2-l2-auth, v0.2-order-builder

## 目标

实现 RFQ (Request for Quote) 系统，支持大宗交易询价功能。

## 需求

RFQ 是一个独立的子客户端，用于大宗交易的询价系统。用户可以发起询价请求，做市商提供报价，用户选择最优报价执行交易。

```zig
const clob = @import("clob");

// 创建 RFQ 请求
const request = try client.rfq.createRfqRequest(.{
    .asset_in = "USDC",
    .asset_out = "token_id_123",
    .amount_in = try Decimal.fromString("10000"),  // 10,000 USDC
    .amount_out = try Decimal.fromString("0"),
    .user_type = 0,
});

// 等待报价
std.time.sleep(5 * std.time.ns_per_s);

// 获取报价
const quotes = try client.rfq.getRfqQuotes(.{
    .request_id = request.request_id,
});

// 获取最佳报价
const best_quote = try client.rfq.getRfqBestQuote(request.request_id);

// 接受报价
const accepted = try client.rfq.acceptRfqQuote(.{
    .request_id = request.request_id,
    .quote_id = best_quote.quote_id,
    .expiration = std.time.timestamp() + 60,  // 1 分钟后过期
});

// 取消请求（如果不想继续）
try client.rfq.cancelRfqRequest(request.request_id);
```

## 任务

- [ ] 定义 RFQ 类型
  - `RfqRequest` / `RfqQuote`
  - `CreateRfqRequestParams`
  - `AcceptQuoteParams`
  - `RfqMatchType` 枚举
  - `RfqConfig`
  
- [ ] 创建 `RfqClient` 子客户端
  - 与主客户端共享认证
  - 实现所有 RFQ 端点
  
- [ ] 实现 RFQ 端点
  - `createRfqRequest()` - POST /rfq/request
  - `cancelRfqRequest()` - DELETE /rfq/request
  - `getRfqRequests()` - GET /rfq/data/requests
  - `createRfqQuote()` - POST /rfq/quote (做市商用)
  - `cancelRfqQuote()` - DELETE /rfq/quote
  - `getRfqQuotes()` - GET /rfq/data/quotes
  - `getRfqBestQuote()` - GET /rfq/data/best-quote
  - `acceptRfqQuote()` - POST /rfq/request/accept
  - `approveRfqOrder()` - POST /rfq/quote/approve
  - `rfqConfig()` - GET /rfq/config
  
- [ ] 在 `ClobClient` 添加 RFQ 子客户端
- [ ] 单元测试
- [ ] 文档更新

## 文件

```
src/
├── rfq/                   # ⏳ 新建目录
│   ├── mod.zig           # RFQ 模块导出
│   ├── client.zig        # RfqClient 实现
│   └── types.zig         # RFQ 类型定义
├── clob/
│   └── client.zig        # 🔨 添加 rfq 子客户端
└── root.zig              # 🔨 添加 rfq 模块导出

docs/
└── rfq/
    └── README.md          # ⏳ 新建: RFQ 文档
```

## 验收标准

- [ ] RFQ 请求创建/取消正常工作
- [ ] RFQ 报价查询正常工作
- [ ] RFQ 报价接受正常工作
- [ ] 所有测试通过：`zig build test`

## 技术细节

### RFQ 匹配类型

```zig
pub const RfqMatchType = enum {
    /// 互补匹配: BUY <> SELL
    COMPLEMENTARY,
    /// 合并匹配: BUY <> BUY 或 SELL <> SELL
    MERGE,
    /// 铸造匹配
    MINT,
    
    pub fn fromString(str: []const u8) ?RfqMatchType {
        if (std.mem.eql(u8, str, "COMPLEMENTARY")) return .COMPLEMENTARY;
        if (std.mem.eql(u8, str, "MERGE")) return .MERGE;
        if (std.mem.eql(u8, str, "MINT")) return .MINT;
        return null;
    }
};
```

### RFQ 请求类型

```zig
pub const RfqRequest = struct {
    request_id: []const u8,
    user_address: []const u8,
    proxy_address: []const u8,
    token: []const u8,
    complement: []const u8,
    condition: []const u8,
    side: []const u8,
    size_in: []const u8,
    size_out: []const u8,
    price: f64,
    accepted_quote_id: ?[]const u8,
    state: []const u8,
    expiry: []const u8,
    created_at: []const u8,
    updated_at: []const u8,
};
```

### RFQ 报价类型

```zig
pub const RfqQuote = struct {
    quote_id: []const u8,
    request_id: []const u8,
    user_address: []const u8,
    proxy_address: []const u8,
    token: []const u8,
    complement: []const u8,
    condition: []const u8,
    side: []const u8,
    size_in: []const u8,
    size_out: []const u8,
    price: f64,
    state: []const u8,
    match_type: []const u8,
    expiry: []const u8,
    created_at: []const u8,
    updated_at: []const u8,
};
```

### 创建请求参数

```zig
pub const CreateRfqRequestParams = struct {
    asset_in: []const u8,
    asset_out: []const u8,
    amount_in: Decimal,
    amount_out: Decimal,
    user_type: u8,
};
```

### 接受报价参数

```zig
pub const AcceptQuoteParams = struct {
    request_id: []const u8,
    quote_id: []const u8,
    expiration: u64,
};
```

### RfqClient 结构

```zig
pub const RfqClient = struct {
    parent: *ClobClient,
    
    pub fn createRfqRequest(
        self: *RfqClient,
        params: CreateRfqRequestParams,
    ) !RfqRequest {
        // POST /rfq/request
    }
    
    pub fn getRfqQuotes(
        self: *RfqClient,
        params: GetQuotesParams,
    ) ![]RfqQuote {
        // GET /rfq/data/quotes
    }
    
    // ... 其他方法
};
```

### API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/rfq/request` | POST | 创建 RFQ 请求 |
| `/rfq/request` | DELETE | 取消 RFQ 请求 |
| `/rfq/data/requests` | GET | 获取 RFQ 请求列表 |
| `/rfq/quote` | POST | 创建 RFQ 报价 (做市商) |
| `/rfq/quote` | DELETE | 取消 RFQ 报价 |
| `/rfq/data/quotes` | GET | 获取 RFQ 报价列表 |
| `/rfq/data/best-quote` | GET | 获取最佳报价 |
| `/rfq/request/accept` | POST | 接受 RFQ 报价 |
| `/rfq/quote/approve` | POST | 批准 RFQ 订单 |
| `/rfq/config` | GET | 获取 RFQ 配置 |

## 备注

- RFQ 功能主要用于大宗交易（通常 $10,000+）
- 做市商需要专门订阅 RFQ 请求并提供报价
- 普通用户主要使用创建请求和接受报价功能
- 参考 Python/TypeScript 客户端的 RFQ 实现

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| - | - | 待开始 |

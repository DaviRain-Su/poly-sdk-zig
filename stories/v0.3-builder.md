# Story: v0.3-builder

> **状态**: ⏳ 待开始
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.3
> **Session**: -
> **依赖**: v0.2-l2-auth

## 目标

实现 Builder API Key 管理功能，支持做市商程序的专用认证流程。

## 需求

Builder 是 Polymarket 的做市商程序认证机制。Builder API Key 具有特殊权限，可以访问做市商专用端点。

```zig
const clob = @import("clob");

// 创建 Builder API Key
const builder_key = try client.createBuilderApiKey();
std.debug.print("Builder API Key: {s}\n", .{builder_key.api_key});

// 获取所有 Builder API Keys
const keys = try client.getBuilderApiKeys();
for (keys.value) |key| {
    std.debug.print("Key: {s}, Created: {s}\n", .{key.api_key, key.created_at});
}

// 撤销 Builder API Key
try client.revokeBuilderApiKey(builder_key.api_key);

// 使用 Builder 配置初始化客户端
var client = ClobClient.initWithBuilder(allocator, .{}, &wallet, &creds, &builder_creds);

// 获取 Builder 交易历史
const trades = try client.getBuilderTrades(.{});
```

## 任务

- [ ] 定义 Builder 相关类型
  - `BuilderApiKey`
  - `BuilderCreds` / `BuilderConfig`
  - `BuilderHeader`
  
- [ ] 实现 Builder API Key 管理端点
  - `createBuilderApiKey()` - POST /auth/builder-api-key
  - `getBuilderApiKeys()` - GET /auth/builder-api-key
  - `revokeBuilderApiKey()` - DELETE /auth/builder-api-key
  
- [ ] 实现 Builder 交易端点
  - `getBuilderTrades()` - GET /builder/trades
  
- [ ] 实现 Builder Header 生成
  - 类似 L2 Header，但包含额外的 Builder 字段
  
- [ ] 扩展 `ClobClient`
  - 添加 `initWithBuilder()` 初始化方法
  - 添加 Builder 端点方法
  
- [ ] 单元测试
- [ ] 文档更新

## 文件

```
src/
├── auth/
│   ├── builder.zig        # ⏳ 新建: Builder 认证
│   └── mod.zig            # 🔨 添加 Builder 导出
└── clob/
    ├── types/
    │   └── builder.zig    # ⏳ 新建: Builder 类型
    └── client.zig         # 🔨 添加 Builder 端点

docs/
└── auth/
    └── builder.md         # ⏳ 新建: Builder 文档
```

## 验收标准

- [ ] Builder API Key 创建/查询/撤销正常工作
- [ ] Builder Header 正确生成
- [ ] Builder 交易查询正常工作
- [ ] 所有测试通过：`zig build test`

## 技术细节

### Builder Header 格式

Builder 请求需要额外的 Header 字段：

```typescript
interface L2WithBuilderHeader extends L2PolyHeader {
    POLY_BUILDER_API_KEY: string;
    POLY_BUILDER_TIMESTAMP: string;
    POLY_BUILDER_PASSPHRASE: string;
    POLY_BUILDER_SIGNATURE: string;
}
```

### Builder 配置

```zig
pub const BuilderCreds = struct {
    api_key: Secret([]const u8),
    api_secret: Secret([]const u8),
    passphrase: Secret([]const u8),
    
    /// 验证配置是否有效
    pub fn isValid(self: *const BuilderCreds) bool {
        return self.api_key.reveal().len > 0 and
               self.api_secret.reveal().len > 0 and
               self.passphrase.reveal().len > 0;
    }
    
    /// 生成 Builder Header
    pub fn generateHeaders(
        self: *const BuilderCreds,
        method: []const u8,
        path: []const u8,
        body: ?[]const u8,
    ) BuilderHeader {
        // HMAC-SHA256 签名，类似 L2
    }
};
```

### API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/auth/builder-api-key` | POST | 创建 Builder API Key |
| `/auth/builder-api-key` | GET | 获取所有 Builder API Keys |
| `/auth/builder-api-key` | DELETE | 撤销 Builder API Key |
| `/builder/trades` | GET | 获取 Builder 交易历史 |

### 响应类型

```zig
pub const BuilderApiKeyResponse = struct {
    api_key: []const u8,
    api_secret: []const u8,
    passphrase: []const u8,
    created_at: []const u8,
};

pub const BuilderTrade = struct {
    id: []const u8,
    // ... 交易详情
};
```

## 备注

- Builder 功能主要面向专业做市商
- Builder API Key 权限高于普通 API Key
- 需要额外的安全考虑（密钥保护等）
- 参考 TypeScript 客户端 `createBuilderApiKey()` 实现

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| - | - | 待开始 |

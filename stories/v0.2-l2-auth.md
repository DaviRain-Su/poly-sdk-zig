# Story: v0.2-l2-auth

> **状态**: ⏳ 待开始
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.2
> **Session**: -
> **依赖**: v0.2-l1-auth

## 目标

实现 L2 级别认证，使用 HMAC-SHA256 签名 API 请求，支持所有需要认证的端点。

## 需求

L2 认证使用 API Key + HMAC-SHA256 签名，用于：
1. 订单管理（创建、取消、查询）
2. 交易历史查询
3. 账户余额查询
4. 通知管理

```zig
const auth = @import("auth");

// 创建 L2 认证器
const l2 = try auth.L2Auth.init(wallet.address, api_creds);

// 生成 L2 Headers
const headers = try l2.generateHeaders(.{
    .method = .POST,
    .path = "/order",
    .body = order_json,
});
// => L2PolyHeader { POLY_ADDRESS, POLY_SIGNATURE, POLY_TIMESTAMP, POLY_API_KEY, POLY_PASSPHRASE }

// 在 HTTP 客户端中使用
const response = try client.post("/order", order_json, .{
    .headers = headers.toHttpHeaders(),
});
```

## 任务

### L2Auth 类型
- [ ] 实现 `L2Auth` 结构
  - `address: Address` - 钱包地址
  - `api_key: []const u8` - API Key
  - `api_secret: Secret([]const u8)` - API Secret
  - `api_passphrase: Secret([]const u8)` - API Passphrase
- [ ] `init(address, creds)` - 初始化
- [ ] `deinit()` - 清理资源

### L2 Header 生成
- [ ] 实现 `L2PolyHeader` 结构
  - `POLY_ADDRESS: []const u8` - 钱包地址
  - `POLY_SIGNATURE: []const u8` - HMAC-SHA256 签名（Base64）
  - `POLY_TIMESTAMP: []const u8` - Unix 时间戳
  - `POLY_API_KEY: []const u8` - API Key
  - `POLY_PASSPHRASE: []const u8` - API Passphrase
- [ ] 实现 `generateHeaders(options)` 方法
- [ ] HMAC-SHA256 签名生成
- [ ] Base64 编码

### HMAC 签名
- [ ] 实现签名消息构造
  - `message = timestamp + method + path + body`
- [ ] 实现 `sign(secret, message)` 函数
- [ ] Base64 编码输出

### HTTP 集成
- [ ] 实现 `toHttpHeaders()` - 转换为 HTTP headers
- [ ] 与 `HttpClient` 集成
- [ ] 自动添加认证 headers

### 测试
- [ ] HMAC 签名正确性测试
- [ ] Header 生成测试
- [ ] 与官方客户端输出对比
- [ ] 时间戳边界测试

### 文档
- [ ] 创建 `docs/auth/l2-auth.md`
- [ ] L2 认证流程说明
- [ ] HMAC 签名算法说明

## 文件

```
src/
└── auth/
    ├── mod.zig           # ⏳ 模块导出
    ├── l2.zig            # ⏳ L2Auth 实现
    ├── headers.zig       # 已创建于 v0.2-l1-auth
    └── hmac.zig          # ⏳ HMAC 签名工具
```

## 技术细节

### HMAC-SHA256 签名算法

```
1. 构造签名消息:
   message = timestamp + method + path + body
   
   示例:
   timestamp = "1704067200"
   method = "POST"
   path = "/order"
   body = '{"tokenId":"123",...}'
   message = "1704067200POST/order{\"tokenId\":\"123\",...}"

2. 计算 HMAC:
   signature = HMAC-SHA256(api_secret, message)

3. Base64 编码:
   encoded = base64(signature)
```

### L2 Header 示例

```http
POLY-ADDRESS: 0x1234567890123456789012345678901234567890
POLY-SIGNATURE: abc123base64signature==
POLY-TIMESTAMP: 1704067200
POLY-API-KEY: your-api-key
POLY-PASSPHRASE: your-passphrase
```

### 使用 L2 认证的端点

| 类别 | 端点 | 方法 |
|------|------|------|
| API Key | `/auth/api-keys` | GET |
| API Key | `/auth/api-key` | DELETE |
| 订单 | `/order` | POST |
| 订单 | `/orders` | POST |
| 订单 | `/data/orders` | GET |
| 订单 | `/data/order/{id}` | GET |
| 订单 | `/order` | DELETE |
| 订单 | `/orders` | DELETE |
| 订单 | `/cancel-all` | DELETE |
| 交易 | `/data/trades` | GET |
| 账户 | `/balance-allowance` | GET |
| 通知 | `/notifications` | GET/DELETE |

### 错误处理

| 错误 | 原因 | 处理 |
|------|------|------|
| 401 | 签名无效 | 检查 HMAC 计算 |
| 401 | 时间戳过期 | 同步服务器时间 |
| 401 | API Key 无效 | 重新获取 API Key |

## 验收标准

- [ ] 能生成正确的 L2 Headers
- [ ] HMAC 签名与官方客户端输出一致
- [ ] 能成功调用需要 L2 认证的端点
- [ ] 时间戳在服务器可接受范围内
- [ ] API Secret 使用 Secret 保护
- [ ] 所有测试通过：`zig build test`

## 参考资料

- [py-clob-client/headers/headers.py](https://github.com/Polymarket/py-clob-client/blob/main/py_clob_client/headers/headers.py)
- [clob-client/src/headers.ts](https://github.com/Polymarket/clob-client/blob/main/src/headers.ts)
- [HMAC - Wikipedia](https://en.wikipedia.org/wiki/HMAC)

## 备注

- L2 认证用于所有需要认证的常规请求
- 时间戳必须是秒级 Unix 时间戳
- 签名消息中的 body 必须是原始 JSON 字符串
- 空 body 时，签名消息中不包含 body 部分
- 考虑添加时间戳缓存，避免频繁获取系统时间

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| - | - | 待开始 |

# Story: v0.1-public-api

> **状态**: ✅ 已完成
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.1
> **Session**: 2024-12-31-005
> **依赖**: v0.1-http, v0.1-types

## 目标

实现只读公共 API 端点（无需认证）。

## 需求

### 客户端用法

```zig
var client = try poly.Client.init(allocator, .{});
defer client.deinit();

// 服务器状态
const ok = try client.ok();

// 市场
const markets = try client.markets(.{});
const market = try client.market("condition_id");

// 价格
const price = try client.price(.{ .token_id = "..." });
const midpoint = try client.midpoint(.{ .token_id = "..." });

// 订单簿
const book = try client.orderBook(.{ .token_id = "..." });
```

### 端点

| 方法 | 端点 | 描述 |
|------|------|------|
| `ok()` | `GET /` | 服务器状态 |
| `serverTime()` | `GET /time` | 服务器时间戳 |
| `markets()` | `GET /markets` | 市场列表 |
| `market(id)` | `GET /markets/{id}` | 单个市场 |
| `price()` | `GET /price` | 代币价格 |
| `midpoint()` | `GET /midpoint` | 中间价 |
| `spread()` | `GET /spread` | 买卖价差 |
| `orderBook()` | `GET /book` | 订单簿 |
| `tickSize()` | `GET /tick-size` | 价格增量 |

## 任务

- [x] 在 `src/clob/types/` 中定义响应类型
  - [x] enums.zig - OrderType, Side, TickSize 等枚举
  - [x] market.zig - Market, Token, SimplifiedMarket 类型
  - [x] book.zig - OrderBookSummary, PriceResponse 等类型
  - [x] mod.zig - 类型模块导出
- [x] 实现 `ClobClient` 结构
- [x] 实现服务器状态端点 (getOk, getServerTime)
- [x] 实现市场数据端点 (getMarkets, getSimplifiedMarkets, getMarket)
- [x] 实现价格和订单簿端点
  - [x] getOrderBook
  - [x] getPrice
  - [x] getMidpoint
  - [x] getSpread
  - [x] getTickSize
  - [x] getNegRisk
  - [x] getLastTradePrice

## 文件

```
src/clob/
├── mod.zig           # ✅ 模块导出
├── client.zig        # ✅ CLOB 客户端
└── types/
    ├── mod.zig       # ✅ 类型模块导出
    ├── enums.zig     # ✅ 枚举类型
    ├── market.zig    # ✅ 市场类型
    └── book.zig      # ✅ 订单簿类型
```

## 已实现端点

| 端点 | 方法 | 状态 |
|------|------|------|
| GET / | getOk() | ✅ |
| GET /time | getServerTime() | ✅ |
| GET /markets | getMarkets() | ✅ |
| GET /simplified-markets | getSimplifiedMarkets() | ✅ |
| GET /markets/{id} | getMarket() | ✅ |
| GET /book | getOrderBook() | ✅ |
| GET /price | getPrice() | ✅ |
| GET /midpoint | getMidpoint() | ✅ |
| GET /spread | getSpread() | ✅ |
| GET /tick-size | getTickSize() | ✅ |
| GET /neg-risk | getNegRisk() | ✅ |
| GET /last-trade-price | getLastTradePrice() | ✅ |

## 验收标准

- [x] 所有公共端点已实现 (12 个)
- [x] 响应类型匹配 API
- [x] 错误被正确处理
- [x] 18 个测试通过

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| 2024-12-31-005 | 2024-12-31 | 完整实现，18 个测试通过 |

# Story: v0.2-signer

> **状态**: ✅ 已完成
> **父级**: [ROADMAP.md](../ROADMAP.md) → v0.2
> **Session**: 2024-12-31-008
> **依赖**: v0.2-crypto

## 目标

实现钱包签名器，支持从私钥创建钱包、EIP-712 结构化数据签名、订单签名。

## 需求

Polymarket 使用 EIP-712 进行订单签名。需要实现：

1. **Wallet** - 钱包类型，包含私钥和地址
2. **EIP-712 签名** - 结构化数据签名标准
3. **订单签名** - 对 CLOB 订单进行签名

```zig
const signer = @import("signer");

// 创建钱包
const wallet = try signer.Wallet.fromPrivateKey("0x...");
std.debug.print("Address: {s}\n", .{wallet.address});

// EIP-712 签名
const typed_data = signer.TypedData{
    .domain = .{
        .name = "Polymarket CTF Exchange",
        .version = "1",
        .chain_id = 137,
    },
    .types = order_types,
    .primary_type = "Order",
    .message = order_message,
};
const signature = try wallet.signTypedData(typed_data);

// 订单签名（便捷方法）
const signed_order = try wallet.signOrder(order, .{
    .chain_id = 137,
    .neg_risk = false,
});
```

## 任务

### Wallet 类型
- [x] 实现 `Wallet` 结构
  - `private_key: Secret(PrivateKey)` - 私钥（使用 Secret 包装）
  - `public_key: PublicKey` - 公钥
  - `address_bytes: [20]u8` - 以太坊地址
- [x] `fromPrivateKey(PrivateKey)` - 从私钥创建
- [x] `fromPrivateKeyHex(hex)` - 从十六进制私钥创建
- [x] `generate()` - 生成随机钱包

- [x] `sign(message_hash)` - 签名 32 字节消息哈希
- [x] `signMessage(message)` - 签名任意消息（EIP-191）
- [x] `verify(message_hash, signature)` - 验证签名
- [ ] ~~`fromMnemonic(mnemonic, path)`~~ - 从助记词派生（推迟到 v0.3）

### EIP-712 实现
- [x] 实现 `Domain` 结构
  - `name`, `version`, `chain_id`, `verifying_contract`, `salt`
- [x] 实现 `TypeField` 和 `TypeDefinition` 结构
- [x] 实现 `encodeDomainType(domain)` - 域类型编码
- [x] 实现 `encodeType(type_def)` - 类型编码
- [x] 实现 `typeHash(type_string)` - 类型哈希
- [x] 实现值编码函数:
  - `encodeUint256()`, `encodeAddress()`, `encodeString()`
  - `encodeBytes32()`, `encodeBool()`, `encodeUint8()`
- [x] 实现 `hashDomain(domain)` - 域分隔符哈希
- [x] 实现 `hashTypedData(domain_separator, struct_hash)` - 完整签名摘要

### 订单签名
- [x] 定义 Polymarket `Order` 结构和 `ORDER_TYPE` 类型定义
- [x] 实现 `orderTypeHash()` - 订单类型哈希
- [x] 实现 `hashOrder(order)` - 订单结构体哈希
- [x] 实现 `createPolymarketDomain(chain_id, neg_risk)` - 创建域分隔符
- [x] 实现 `hashPolymarketOrder(order, chain_id, neg_risk)` - 完整订单签名摘要
- [x] 定义合约地址常量:
  - `CTF_EXCHANGE_ADDRESS_MAINNET`, `CTF_EXCHANGE_ADDRESS_AMOY`
  - `NEG_RISK_CTF_EXCHANGE_ADDRESS_MAINNET`, `NEG_RISK_CTF_EXCHANGE_ADDRESS_AMOY`
  - `POLYGON_CHAIN_ID`, `AMOY_CHAIN_ID`

### 签名类型支持
- [x] EOA 签名（signature_type = 0）- 支持
- [ ] ~~POLY_PROXY 签名（signature_type = 1）~~（推迟到 v0.3）
- [ ] ~~POLY_GNOSIS_SAFE 签名（signature_type = 2）~~（推迟到 v0.3）

### 测试
- [x] Wallet 测试 (10 tests)
  - 创建/生成钱包
  - 地址计算
  - 签名和验证
  - EIP-191 消息签名
  - Secret 保护
- [x] EIP-712 测试 (15 tests)
  - 类型编码
  - 值编码
  - 域哈希
  - 订单哈希
  - Polymarket 域
- [x] 模块集成测试 (2 tests)

### 文档
- [x] 创建 `docs/signer/README.md` - 模块概述
- [x] 创建 `docs/signer/wallet.md` - Wallet 详细文档
- [x] 创建 `docs/signer/eip712.md` - EIP-712 详细文档

## 文件

```
src/
└── signer/
    ├── mod.zig           # ✅ 模块导出
    ├── wallet.zig        # ✅ Wallet 类型 (10 tests)
    └── eip712.zig        # ✅ EIP-712 + 订单签名 (15 tests)
```

## 技术细节

### EIP-712 签名流程

```
1. hashStruct(typeName, data):
   - typeHash = keccak256(encodeType(typeName))
   - dataHash = keccak256(encodeData(typeName, data))
   - return keccak256(typeHash || dataHash)

2. 完整签名:
   - domainSeparator = hashStruct("EIP712Domain", domain)
   - structHash = hashStruct(primaryType, message)
   - digest = keccak256("\x19\x01" || domainSeparator || structHash)
   - signature = sign(digest)
```

### Polymarket 订单 EIP-712 类型

```javascript
const orderTypes = {
    Order: [
        { name: "salt", type: "uint256" },
        { name: "maker", type: "address" },
        { name: "signer", type: "address" },
        { name: "taker", type: "address" },
        { name: "tokenId", type: "uint256" },
        { name: "makerAmount", type: "uint256" },
        { name: "takerAmount", type: "uint256" },
        { name: "expiration", type: "uint256" },
        { name: "nonce", type: "uint256" },
        { name: "feeRateBps", type: "uint256" },
        { name: "side", type: "uint8" },
        { name: "signatureType", type: "uint8" },
    ],
};

const domain = {
    name: "Polymarket CTF Exchange",
    version: "1",
    chainId: 137,
    verifyingContract: "0x4bFb41d5B3570DeFd03C39a9A4D8dE6Bd8B8982E",
};
```

### 签名类型说明

| 类型 | 值 | 使用场景 | 特殊处理 |
|------|---|----------|----------|
| EOA | 0 | MetaMask, 硬件钱包 | 标准签名 |
| POLY_PROXY | 1 | Email/Magic 钱包 | 需要 funder 地址 |
| POLY_GNOSIS_SAFE | 2 | 代理合约 | 需要 funder 地址 |

## 验收标准

- [x] 能从私钥创建 Wallet
- [x] 地址计算正确（与以太坊工具对比）
- [x] EIP-712 哈希计算正确
- [ ] ~~订单签名可被 Polymarket API 接受~~（需要集成测试验证，v0.3）
- [x] 支持 EOA 签名类型
- [x] 所有测试通过：`zig build test` (168 tests)
- [x] 私钥使用 Secret 包装，不会意外泄露

## 参考资料

- [EIP-712: Typed structured data hashing and signing](https://eips.ethereum.org/EIPS/eip-712)
- [EIP-191: Signed Data Standard](https://eips.ethereum.org/EIPS/eip-191)
- [py-clob-client/signer.py](https://github.com/Polymarket/py-clob-client/blob/main/py_clob_client/signer.py)
- [clob-client/src/signing/eip712.ts](https://github.com/Polymarket/clob-client/blob/main/src/signing/eip712.ts)

## 备注

- 私钥是高度敏感数据，必须使用 `Secret` 类型
- 签名流程必须与官方实现完全一致，否则 API 会拒绝
- 考虑添加签名验证功能，用于本地测试

## Session 日志

| Session | 日期 | 进度 |
|---------|------|------|
| 2024-12-31-008 | 2024-12-31 | ✅ 完成 - 创建 mod.zig, 更新 root.zig, 修复 API 兼容性, 创建文档 |
| 2024-12-31-007 | 2024-12-31 | 创建 wallet.zig, eip712.zig (由 v0.2-crypto 会话开始) |
